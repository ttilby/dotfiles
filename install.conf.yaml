- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vimrc: .vimrc

- create:
    - ~/Downloads
    - ~/.vim/sessions
    - ~/.vim/undodir

- shell:
  - [git submodule update --init --recursive, Install submodules]
